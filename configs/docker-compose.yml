services:
  frontend:
    image: username/arcis_frontend_r-d:latest
    ports:
      - "80:80"
    depends_on:
      - backend

  backend:
    image: username/arcis_backend_r-d:latest
    env_file:
      - ./backend/.config.env
    ports:
      - "8082:8082"
